# Build output
build/
build-fuzz/
build-san/

# Fuzzing artifacts (crashes and any local scratch corpus are not versioned)
crash-*
leak-*
timeout-*
oom-*
slow-unit-*

# Editor / OS
.vscode/
.DS_Store
